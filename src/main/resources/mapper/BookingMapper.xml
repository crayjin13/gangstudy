
	
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.jts.gangstudy.mapper.BookingMapper">

	<!-- 전체 내용 조회 -->
	<select id="viewAll" resultType="com.jts.gangstudy.domain.Booking">
	  SELECT * FROM BOOKING_TB
	</select>
	
	<!-- 기존 예약 목록 조회 -->
	<select id="existsBooking" resultType="com.jts.gangstudy.domain.Booking">
	  SELECT * FROM BOOKING_TB WHERE BOOK_DT = #{book_dt}
	</select>
</mapper>
